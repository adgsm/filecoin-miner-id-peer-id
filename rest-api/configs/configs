[postgresql]
postgresql_host = localhost
postgresql_port = 5432
postgresql_database = filecoin_miner_id_peer_id
postgresql_user = filecoin_miner_id_peer_id
postgresql_password = FILECOIN_MINER_ID_PEER_ID_DATABASE_PASSWORD

[log]
logfile = logs/log

[tsl]
tsl_key = localhost.key
tsl_cert = localhost.crt

[api]
api_host = localhost
api_port = 3000
